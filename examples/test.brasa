diga("Digite seu nome: ");
texto name := leia();

func welcome(texto name)->texto{
  texto welcome_message := "Welcome, " + name + "!";

  retorne welcome_message;
}

texto result := welcome(name);
diga(result);
